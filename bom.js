// Accessing the window object
console.log(window.innerWidth); // Get the width of the browser window

// Accessing the document object
document.getElementById("myElement").textContent = "Hello, BOM!";

// Navigating to a different URL
window.location.href = "https://www.example.com";

// Using the navigator object
console.log(navigator.userAgent); // Get the user agent string

// Accessing the history object
window.history.back(); // Go back to the previous page

// Using the screen object
console.log(screen.width); // Get the screen width
